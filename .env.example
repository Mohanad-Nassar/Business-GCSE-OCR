SUPABASE_URL=https://eaohjlyiotyqhvsizcpw.supabase.co
SUPABASE_SERVICE_ROLE_KEY=
MARKING_GEMINI_API_KEY=

# Secret that places the correct option in generated match questions.
# REQUIRED: the bank build refuses to run without it, because otherwise
# every match answer is derivable from the printed term via djb2(term)%4.
# Generate with: python -c "import secrets;print(secrets.token_urlsafe(32))"
# Must be identical across machines or the option order will churn.
BANK_SHUFFLE_SALT=
